import React, { Component } from 'react'

// Define component that doesn't need any logic -- just presentation
const Title = function(props) {
 return (
   <div>{props.name}</div>
 )
}

export default Title;
